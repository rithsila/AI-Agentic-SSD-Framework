# Stack Conventions

> TEMPLATE — fill with this project's locked facts. Generated by `/spec rules` from `PRD.md §5`.
> Pure project facts only: versions, paths, commands. Rationale lives in `PRD.md`, not here.
> See a filled instance in `examples/ali-store/.claude/rules/stack.md`. Keep under ~80 lines.

## Tech stack (locked)
- **Backend**: <framework + EXACT version>, <runtime>, TypeScript (strict)
- **Frontend**: <framework + version>
- **Styling**: <approach + where tokens come from (point at DESIGN.md)>
- **Database**: <engine — dev vs prod, how the URL is set>
- **Cache/events**: <if any, else remove>
- **File storage**: <if any, else remove>
- **Payments**: <provider(s) and integration shape, or "none">
- **Auth**: <admin strategy + customer strategy>
- **Notifications**: <if any, else remove>
- **Hosting**: <where backend runs / where frontend runs>
- **Language(s)**: <v1 scripts + fonts; note any v1→v2 split>

## Repos
- `<repo-a>/` — <what it is>
- `<repo-b>/` — <what it is>

## Backend file organization (`<repo>/src/`)
- `<dir>/` — <purpose>

## Frontend file organization (`<repo>/src/`)
- `<dir>/` — <purpose>

## Conventions
- <data-fetching rule — which client, which key, what the frontend must never call>
- <forms rule>
- <tokens rule — use named tokens from DESIGN.md, no ad-hoc values>
- <migrations rule — generate files only; humans apply to prod>
- <validation rule — zod (or equivalent) at every boundary before the service layer>
- <errors rule — log server-side with correlation id; generic shape to client>
- **Dependencies**: pin exact (no `^`/`~`), commit lockfiles, install with `<ci command>`. Vendor security-critical small packages.
- **No new dependencies** without explicit approval; the locked stack above is the allowed set.

## Environment variables
Defined in `.env.example` (no values committed).
- Backend: `<VAR>`, `<VAR>`, …
- Frontend: `<VAR>`, …

## Commands
- Dev: `<cmd>`
- Build: `<cmd>`
- Generate migration: `<cmd>`
- Run migration (dev only): `<cmd>`
- Lint: `<cmd>`
- Test: `<cmd>`
- Install (CI + deploy): `<cmd>` — never the mutable-install variant in those paths
